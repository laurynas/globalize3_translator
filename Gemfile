source :rubygems

gem 'activerecord', '>= 3.0.0.rc', :require => 'active_record'
gem 'globalize3', '>= 0.0.7'
gem 'sishen-rtranslate', '>= 1.3'

group :test do
  gem 'ruby-debug'
  gem 'sqlite3-ruby'
end
